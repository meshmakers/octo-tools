apiVersion: v1
kind: Service
metadata:
  name: mongodb
  namespace: octo-infra
  labels: { app: mongodb }
spec:
  clusterIP: None
  selector: { app: mongodb }
  ports:
    - { name: mongo, port: 27017, targetPort: 27017 }
---
apiVersion: v1
kind: Service
metadata:
  name: mongodb-ext
  namespace: octo-infra
spec:
  type: NodePort
  selector: { app: mongodb }
  ports:
    - { name: mongo, port: 27017, targetPort: 27017, nodePort: 30017 }
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mongodb
  namespace: octo-infra
spec:
  serviceName: mongodb
  replicas: 1
  selector: { matchLabels: { app: mongodb } }
  template:
    metadata: { labels: { app: mongodb } }
    spec:
      securityContext:
        runAsUser: 999
        runAsGroup: 999
        fsGroup: 999
      initContainers:
        - name: copy-keyfile
          image: mongo:8.0.12
          command:
            - "sh"
            - "-c"
            - "cp /secret/file.key /keyfile/file.key && chmod 400 /keyfile/file.key && chown 999:999 /keyfile/file.key"
          securityContext: { runAsUser: 0 }
          volumeMounts:
            - { name: keyfile-secret, mountPath: /secret, readOnly: true }
            - { name: keyfile, mountPath: /keyfile }
      containers:
        - name: mongodb
          image: mongo:8.0.12
          command:
            - "mongod"
            - "--keyFile"
            - "/keyfile/file.key"
            - "--replSet"
            - "rs"
            - "--bind_ip_all"
            - "--wiredTigerCacheSizeGB"
            - "2"
          ports:
            - { containerPort: 27017, name: mongo }
          volumeMounts:
            - { name: keyfile, mountPath: /keyfile }
            - { name: data, mountPath: /data/db }
            - { name: init, mountPath: /scripts }
          readinessProbe:
            exec:
              command: ["sh", "-c", "mongosh --quiet --eval 'db.adminCommand({ ping: 1 })'"]
            initialDelaySeconds: 10
            periodSeconds: 10
            # An exec probe without timeoutSeconds gets Kubernetes' default of 1 s. Measured on an
            # idle local cluster this command takes ~0.25-0.42 s, so the default leaves under a
            # factor of three of headroom — and a machine under load reaches that easily. When it
            # does, the probe does not report a sick database: it CUTS OFF a healthy one, the pod
            # leaves the Service endpoints, and every client sees END_OF_STREAM at once. Observed
            # exactly that on 2026-09-14/15 while the host was saturated.
            timeoutSeconds: 5
      volumes:
        - { name: keyfile-secret, secret: { secretName: mongodb-keyfile } }
        - { name: keyfile, emptyDir: {} }
        - { name: init, configMap: { name: mongodb-init } }
  volumeClaimTemplates:
    - metadata: { name: data }
      spec:
        accessModes: ["ReadWriteOnce"]
        resources: { requests: { storage: 5Gi } }
