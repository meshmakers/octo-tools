apiVersion: apps/v1
kind: Deployment
metadata:
  name: rabbitmq
  namespace: octo-infra
  labels: { app: rabbitmq }
spec:
  replicas: 1
  selector: { matchLabels: { app: rabbitmq } }
  template:
    metadata: { labels: { app: rabbitmq } }
    spec:
      containers:
        - name: rabbitmq
          image: rabbitmq:4.0.6-management
          env:
            - { name: RABBITMQ_DEFAULT_USER, value: "guest" }
            - { name: RABBITMQ_DEFAULT_PASS, value: "guest" }
          ports:
            - { containerPort: 5672,  name: amqp }
            - { containerPort: 15672, name: management }
          readinessProbe:
            exec: { command: ["rabbitmq-diagnostics", "-q", "ping"] }
            initialDelaySeconds: 15
            periodSeconds: 10
            # Same reasoning as mongodb.yaml: without timeoutSeconds this exec probe gets 1 s.
            # Measured ~0.3 s idle, so a loaded host cuts off a healthy broker and takes it out of
            # the Service endpoints.
            timeoutSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: rabbitmq
  namespace: octo-infra
spec:
  type: NodePort
  selector: { app: rabbitmq }
  ports:
    - { name: amqp,       port: 5672,  targetPort: 5672,  nodePort: 30672 }
    - { name: management, port: 15672, targetPort: 15672, nodePort: 31672 }
